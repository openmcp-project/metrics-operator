---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.12.0
  name: metrics.business.orchestrate.cloud.sap
spec:
  group: business.orchestrate.cloud.sap
  names:
    kind: Metric
    listKind: MetricList
    plural: metrics
    singular: metric
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: Metric is the Schema for the metrics API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: MetricSpec defines the desired state of Metric
            properties:
              fieldSelector:
                type: string
              foo:
                description: Foo is an example field of Metric. Edit metric_types.go
                  to remove/update
                type: string
              kind:
                type: string
              labelSelector:
                type: string
              name:
                type: string
            type: object
          status:
            description: MetricStatus defines the observed state of Metric
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
