# permissions for end users to edit remoteclusteraccesses.
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/name: co-metrics-operator
    app.kubernetes.io/managed-by: kustomize
  name: remoteclusteraccess-editor-role
rules:
- apiGroups:
  - orchestrate.cloud.sap
  resources:
  - remoteclusteraccesses
  verbs:
  - create
  - delete
  - get
  - list
  - patch
  - update
  - watch
- apiGroups:
  - orchestrate.cloud.sap
  resources:
  - remoteclusteraccesses/status
  verbs:
  - get
